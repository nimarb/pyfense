# pyfense_level.py
# contains PyFenseLevel class (layer)
# contains and handles the map selector

import cocos
from cocos import layer

class PyFenseLevel(layer.Layer):
    def __init__(self):
        super(PyFenseLevel, self).__init()__
